# --- Core Application ---
streamlit==1.32.0
fastapi==0.110.0
uvicorn==0.27.1
python-dotenv==1.0.1
watchfiles==0.21.0

# --- Database & Vectors ---
# We PIN numpy<2 to prevent the "Numpy 2.0" crash on Render
numpy<2.0.0
chromadb==0.4.24
# We use 2.7.0 to satisfy langchain-huggingface requirements
sentence-transformers==2.7.0
rank_bm25==0.2.2
huggingface-hub==0.23.2

# --- AI & LangChain (The "Golden" Compatible Set) ---
# We stick to the 0.1.x series because it is the most stable for your code
cohere==5.5.8
langchain==0.1.20
langchain-community==0.0.38
langchain-core==0.1.52
langchain-text-splitters==0.0.1
langchain-groq==0.1.3
langchain-cohere==0.1.4
langchain-huggingface==0.0.3

# --- File Processing ---
pypdf==4.1.0
PyMuPDF==1.23.26

# --- Utilities ---
typing_extensions==4.12.0
pydantic==2.6.3
requests==2.31.0